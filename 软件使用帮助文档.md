# VideoKit 软件使用帮助文档

> 适用范围：当前 VideoKit 桌面版。本文以软件界面中的实际功能为准；不同系统、账户权限、网络状况和已安装组件可能使个别功能不可用或处理速度不同。

## 1. 开始使用

VideoKit 用于配音、字幕处理、视频下载、媒体转换与批量短视频制作。顶部导航栏提供以下主界面：

| 界面 | 用途 |
| --- | --- |
| 一键配音字幕 | 批量生成配音，并输出带时间轴字幕的结果。 |
| ElevenLabs | 文本转语音、批量 TTS 和音效生成。 |
| 字幕对齐 | 将原文、译文与音视频识别结果对齐成字幕。 |
| 媒体转换 | 格式转换、剪辑、画面与音频处理、素材整理。 |
| SRT 工具 | 调整字幕时间与生成无缝字幕。 |
| 视频下载 | 解析并下载视频链接。 |
| 批量 Reels | 批量制作带字幕、覆层、水印和背景的短视频。 |
| 设置 | 配置 API、输出目录、主题、缓存和更新。 |

### 1.1 通用操作

1. 需要文件的界面通常都支持“选择文件/文件夹”，部分支持直接拖拽。
2. 输出目录留空时，软件会使用当前功能的默认位置（通常为源文件目录或“设置”中的默认输出目录）。重要任务建议手动指定独立输出目录。
3. 批量任务请先用一条或一个文件验证参数，再处理全部素材。
4. 关闭复制模式、直接替换音频、重命名原文件等操作会影响原始素材；请先备份。
5. 使用 ElevenLabs、Gladia 或 Gemini 的功能前，先在“设置”填入相应凭证并保存。

## 2. 一键配音字幕

该页用于把多条文案批量转为语音，并基于音频生成对齐字幕。

### 操作步骤

1. 在“默认音色”选择声音，必要时点击刷新加载最新音色列表。
2. 选择模型、对齐语种、最大片段长度、尾部静音和并发任务数。
3. 指定输出目录；可点击“浏览”选择，完成后用“打开”进入输出位置。
4. 在表格中填写文案，或点击“从表格粘贴”粘贴 Google Sheets / Excel 中的一列或多列内容。
5. 检查任务后开始生成；进度和结果会显示在任务列表中。

### 参数说明

| 参数 | 说明 |
| --- | --- |
| 默认音色 | 该批任务默认使用的 ElevenLabs 声音。 |
| 模型 | 决定语音生成能力、速度与费用；按账户可用模型选择。 |
| 对齐语种 | 用于音频与文案的时间对齐，应与实际朗读语言一致。 |
| 最大片段 | 长文案的切分上限；过长时会分段生成。 |
| 尾部静音 | 为每条语音末尾补留白，便于视频衔接。 |
| 并发任务 | 同时处理的任务数量；过高可能触发 API 限流。 |

## 3. ElevenLabs

该页提供 ElevenLabs 账号连接、文本转语音、批量 TTS 以及音效生成。

### 3.1 账号连接和 API Keys

支持网页登录/重新登录、使用浏览器打开登录页后粘贴 Token，以及直接保存 API Keys。连接后可刷新音色与额度信息。若连接异常，先使用“验证连接”，再检查 Token 或 Key 是否过期。

### 3.2 文本转语音（TTS）

1. 在“搜索声音”输入名称并搜索，选择声音后可点击“试听”。
2. 选择模型，输入需要朗读的文本；“自动断行”可帮助将较长文案分行处理。
3. 选择保存位置并点击“生成语音”。

### 3.3 批量生成（TTS）

通过“添加”新增任务行，或从表格粘贴多条文案。确认每行的文本、音色和输出名称后点击“批量生成”。“清空”仅清除当前任务列表，不会删除已生成文件。

### 3.4 音效生成（SFX）

输入音效描述，设置时长和保存位置后生成。例如可描述“雨夜的城市环境声”“轻快的按钮提示音”。描述应包含场景、声音主体和氛围，避免只输入笼统词语。

## 4. 字幕对齐

该页将音频/视频中的语音识别时间轴与原文、译文对齐，用于生成双语或翻译字幕。

1. 在 Step 1 导入音视频或 JSON 文件。
2. 在 Step 2 粘贴或加载原文本，并按希望的字幕显示方式断行。
3. 在 Step 3 粘贴译文；译文需要手动提供或从已有内容加载。
4. 在 Step 4 设置识别语言和音频切分时长，启动处理。

批量模式中可通过“粘贴原文+译文”“粘贴原文”或“粘贴译文”快速导入表格数据。处理前请确认原文与译文的段落顺序一致。该功能需要可用的 Gladia API Key。

## 5. SRT 工具

### 5.1 时间调整

用于根据字符时长重新分配 SRT 字幕时间。

1. 选择源 SRT；如需参考现有时间轴，可额外选择原 SRT 和参考 SRT。
2. 设置间隔时间、单字符时间、短句子字符数、字符时间倍数和排除字符。
3. 可先点击“计算字符时间”估算参数，再点击“开始调整”。

适合字幕过密、阅读时间不均或翻译后长度变化明显的情况。结果生成后请抽检长句、短句和标点行。

### 5.2 无缝字幕

用于减少相邻字幕之间不必要的空隙，使字幕显示更连续。选择文件后点击“生成无缝字幕”，再用播放器核对换句位置是否自然。

## 6. 媒体转换

左侧按功能分组。选择功能后，在右侧完成参数设置、选择输入/输出位置并启动处理。

### 6.1 格式转换

| 功能 | 使用方法与结果 |
| --- | --- |
| H.264（MP4） | 选择视频后转为通用 MP4，兼容上传、预览和跨平台播放。 |
| H.264 压缩 | 调低体积或码率；先用单个样片确认画质。 |
| DNxHR（MOV） | 输出适合部分后期流程的 MOV 中间格式，文件通常较大。 |
| DNxHR 10bit | 输出更高色深的 DNxHR MOV；需要更大存储空间与兼容的剪辑软件。 |
| 图片转 PNG/JPG/JPEG | 将图片批量转换为目标格式；JPG/JPEG 可按需要设置质量。 |

### 6.2 Logo 叠加

选择预设 Logo 或自定义 Logo 图片，设置 X、Y、宽、高后查看 9:16 竖屏预览并导出。预设坐标以 1080×1920 竖屏为基准；横屏素材需通过预览复核位置。

### 6.3 文字水印

输入文字，设置预设或自定义字体、字号、颜色、透明度、描边、位置和 X/Y 微调，然后查看竖屏预览并转换。水印会重新编码视频；批量任务建议先测试一条。

### 6.4 音频处理

| 功能 | 操作要点 |
| --- | --- |
| 提取音频 MP3 | 从视频或音频提取声音并输出 MP3。 |
| 音频转 WAV | 转换为无压缩 WAV，适合后续识别与编辑。 |
| 音频转黑屏 MP4 | 把音频封装为黑屏视频，适用于只接受视频的平台。 |
| 音频裁切分割 | 导入音频，按裁切点拆分为多个片段。 |
| 音频特效处理 | 选择场景、混响量、立体声扩展与输出格式后处理。 |
| 音频检测匹配 | 导入对比音频/视频，分析对齐点，试听原音/变声音频/对齐后结果，微调 ±10 或 ±50ms 后导出替换视频声音。 |

音频检测匹配的推荐流程是：导入文件 → 开始分析对比 → 自动剪辑对齐 → 试听 → 必要时微调 → 导出并替换视频。不要在未试听的情况下直接覆盖成片。

### 6.5 场景检测

导入一个或多个视频，设置“灵敏度”和“最小间隔”，可选导出时添加文字或图片水印。灵敏度越低通常检测到的镜头切换越多；最小间隔用于过滤过于密集的切点。结果可导出场景片段或每场景的图片帧，并可设置输出目录、帧数、图片格式和 JPG 质量。

### 6.6 智能关键帧

支持“本地视频”和“视频链接”两种来源。

1. 选择视频或粘贴链接（每行一个）。
2. 设置输出目录、场景检测灵敏度、最小间隔、每场景额外采样、图片格式和 JPG 质量。
3. 处理后在视频预览中用上一帧/下一帧检查标记，可添加、删除或清空标记。
4. 在帧网格中选择需要导出的关键帧。

### 6.7 批量截图

可从本地视频文件夹（支持递归子文件夹）或链接列表截图。设置截图模式、图片格式和 JPG 质量，指定输出目录后执行。处理完成后在“处理进度”和“截图结果”区域查看结果。

### 6.8 智能聚类分类

选择图片/视频文件夹，指定输出目录，设置相似度阈值、最小分组和文件操作方式，然后开始分类。相似度阈值高会使分组更严格、组数可能更多；选择移动文件会改变原素材位置，建议先使用复制方式。

### 6.9 批量剪辑

选择一个或多个视频，在剪辑片段列表中添加、编辑或粘贴入点/出点。随后设置剪辑选项和 FCPXML 字幕样式，查看剪辑点预览并执行。若需要导入剪辑软件继续编辑，可导出 FCPXML 时间线。

### 6.10 文案自动剪辑

把多个拍摄片段与最终文案匹配，自动保留对应语音并拼接，同时生成字幕。

1. 选择“单套”或“批量任务”。批量任务可添加多个文件夹，设置同时分析的套数。
2. 选择视频片段，粘贴“断行文案”，并按需要打开已有分析项目。
3. 设置语言、匹配方式、工作流、输出规格、画面适配、转场及转场时长、前后留白和匹配阈值。
4. 可设置音色、稳定度、相似度，或选择手动变声音频。
5. 先执行分析并在“自动剪辑结果”中审核；确认后正式导出，或选择“仅导出视频 + 字幕”。

该功能需要 Gladia。断行决定字幕显示，通常不等于视频片段边界；如使用“一行一片段”兼容模式，才会严格按行匹配。

### 6.11 批量 TXT 与命名工具

| 功能 | 操作说明 |
| --- | --- |
| TXT 智能断行 | 粘贴表格文案，设置每行最大字符数，生成便于字幕或朗读的断行文本。 |
| 批量 TXT | 从 Google Sheets / Excel 粘贴一列文案，设置起始编号、补零位数和输出目录，批量生成 TXT。 |
| 统一命名 | 选择多种格式的同组文件，以选定文件名或手动输入的统一名称作为基础名称。关闭复制模式会重命名原文件。 |
| 批量重命名 | 选择文件后，按“查找/替换、前缀/后缀、基础名称、序号位置、起始编号、编号位数”组合规则执行。先核对预览列表。 |

## 7. 视频下载

### 7.1 单链接解析

1. 粘贴视频链接，点击“解析链接”。
2. 在解析结果中选择视频条目。
3. 设置格式、画质、线程数和保存位置。
4. 启动下载，并在“总进度”区域查看状态。

### 7.2 批量粘贴链接

每行粘贴一个链接，解析后在任务列表中检查条目，统一设置格式、画质、文件名规则和保存位置后下载。下载日志用于查看失败原因与完成记录。

请仅下载您拥有权利或获授权下载的内容，并遵守平台规则与所在地法律。

## 8. 批量 Reels

这是完整的短视频批量编辑器：中间为预览和时间线，左/下方用于素材、任务和输出，右侧检查器用于样式设置。典型流程是：导入素材 → 编辑字幕 → 添加覆层/水印/背景 → 预览 → 选择输出参数 → 批量渲染。

### 8.1 工具栏、素材与任务

工具栏可用于导入、保存、导出/导入模板、打开更多工具和管理队列。批量表格用于查看并选择任务；可全选、反选或取消选择。保存可保留当前工作状态；导入/导出便于复用字幕与视觉模板。

### 8.2 字幕检查器

“T 字幕”页控制字幕内容、全局样式、关键词高亮、背景框、排版与动画。

| 设置组 | 可控制内容 |
| --- | --- |
| 基础文字 | 字体、字号、粗体/斜体、字重、字间距、方向、字体系、文字颜色。 |
| 高亮与描边 | 普通文字与高亮颜色、描边开关/宽度、阴影开关/模糊/X/Y。 |
| 背景框 | 背景框开关、自适应宽度、透明度、圆角、羽化、内距、动态框、渐变与过渡。 |
| 文本区域 | 高级文本框的 X/Y/宽/高、对齐、可见行数、行距、旋转与位置。 |
| 关键词规则 | 可添加关键词、数字高亮、英文高亮；用于自动强调指定文本。 |
| 动画 | 入场/出场、打字机、弹跳、漂浮、节奏、缩放、闪光/圣光、模糊和随机散射等。 |

建议先选择“动画预设”或“全样式预设”，再微调细节。样式作用域决定修改应用于当前任务还是批量任务。所有效果都应在预览中确认，尤其是不同长度文案的换行。

### 8.3 覆层检查器

“🖼️ 覆层”页用于添加图片、视频或其他叠加元素。通过“覆层编辑”设置元素的位置、尺寸、层级、透明度和动画；需要统一多条视频时，可使用批量应用或保存为组合预设。

### 8.4 AI 水印检查器

“🔒 水印”页提供水印预设和组合管理。可选择预设、保存组合、删除组合、导入或导出水印配置。请在不同画幅和较亮/较暗的素材上检查可读性。

### 8.5 背景与内容视频检查器

“🌫️ 背景与内容视频”页管理背景、内容视频和配乐。

| 区域 | 功能 |
| --- | --- |
| 背景 | 选择单个背景素材、同内容作为背景，或使用背景片段池并按规则拼接；可设置转场、缩放、翻转、位置、音量和时长。 |
| 内容视频 | 选择内容视频或同背景；可开启毛玻璃背景、直接铺满、调整模糊、亮度、音量、缩放、翻转和截取时间段。 |
| 画幅与裁切 | 使用全画、16:9、4:5、1:1、9:16、1:2、半高等预设，或精确输入左、上、宽、高。 |
| 配乐 | 选择单个配乐或配乐池，设置组合方式和音量。 |

“管理素材池”用于维护可随机或组合使用的背景/配乐文件；“拼接并设为背景”可先将背景池生成一个背景素材。注意人声、背景层和配乐层音量的平衡，避免人声被掩盖。

### 8.6 预览、渲染与输出

使用播放控制和时间线检查字幕开始/结束、覆层位置和音画同步。输出区域可设置输出目录、画质、目标/最大参数、后缀与 GPU 加速；支持“极速内存渲染”。GPU 状态会显示在界面中，若检测失败可改用兼容的渲染方式。开始批量渲染前，先选中需要导出的任务并用一条样片核验。

## 9. 设置

### 9.1 API 与账号

| 配置 | 用途 |
| --- | --- |
| ElevenLabs 配置 | 登录、保存 Token/API Key、获取音色与调用 TTS/SFX。 |
| Gladia API Keys | 语音识别、字幕对齐、文案自动剪辑。 |
| Gemini 配置 | AI 处理相关能力；可先“测试 Key”再保存。 |
| 翻译文本替换规则 | 按语言维护替换规则，统一处理常用译名、符号或格式。 |

Key 和 Token 属于敏感信息，不应粘贴到文案、截图或共享的项目文件中；失效后请在对应设置中更新。

### 9.2 界面与存储

- 主题颜色：可自定义，使用“恢复默认”回到初始主题。
- 界面缩放：可选 80% 至 150%，用于适配不同分辨率和阅读需求。
- 默认输出目录：所有支持默认输出目录的功能会优先使用此处设置。
- 缓存管理：可打开缓存位置、更改位置、恢复默认、清理缓存或刷新信息。清理缓存不会删除主动导出的成片，但会移除可重新生成的临时数据。

### 9.3 关于与更新

可选择更新通道，并从历史版本下载区域获取已审核来源的安装包。升级前请保存正在编辑的批量 Reels 项目和重要模板。

## 10. 常见问题

### API 功能无法使用

确认对应 Key/Token 已保存且仍有效，检查网络连接与账户额度，然后用“测试 Key”或“验证连接”重新验证。若批量任务失败，降低并发任务数后重试。

### 字幕对不齐或自动剪辑结果不理想

确认识别语言、输入音频质量和文案顺序正确。降低噪声、避免多人同时说话，并把长文案合理断行。自动剪辑应先分析和审核，必要时调整匹配阈值、前后留白或转场后再导出。

### 导出很慢或失败

检查磁盘空间、输出路径写入权限和源文件是否能正常播放。先关闭不必要的任务，测试单个短样片；若 GPU 不可用或异常，使用更兼容的渲染选项。

### 找不到输出文件

先检查当前界面是否手动设置了输出目录，其次检查“设置 → 默认输出目录”和源文件目录。可使用界面的“打开”按钮直接定位输出位置。

### 重命名后如何恢复

软件不会保证对原名提供撤销记录。批量重命名与统一命名操作前应复制素材或启用复制模式，并先对少量文件验证规则。

